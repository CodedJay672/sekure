import LoadingSpinner from "@/components/Alert/Loading";
import Modal from "../Modal";

const LoadingUI = () => {
  return (
    <Modal>
      <LoadingSpinner />
    </Modal>
  );
};

export default LoadingUI;
